@inject IJSRuntime JSRuntime
@rendermode InteractiveServer

@inject Blazored.SessionStorage.ISessionStorageService sessionStorage
@inherits LayoutComponentBase

@inject LoginState loginState;
@implements IDisposable

@inject NavigationManager navigationManager



<nav id="fileTabs" class="navbar navbar-expand-lg bg-body-tertiary" style="display:none" >
    <div class="container-fluid">
        <button class="navbar-brand" @onclick="@(() => navigationManager.NavigateTo("mytasks/search"))" href="#" Match="NavLinkMatch.All">Search</button>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <button id="file-tab" class="nav-link active" @onclick="@(() => navigationManager.NavigateTo("accessrequests/add"))" href="#">Access Request</button>
                <button id="payment-tab" class="nav-link disabled" @onclick="@(() => navigationManager.NavigateTo("feepayment/add"))" href="#">Payment</button>
                <button id="disclosure-tab" class="nav-link disabled" @onclick="@(() => navigationManager.NavigateTo("disclosure/add"))" href="#">Disclosure</button>
                <button id="extension-tab" class="nav-link disabled" @onclick="@(() => navigationManager.NavigateTo("extension/add"))" href="#">Extension</button>
               
            </div>
        </div>
    </div>
</nav>


@code {
    private string? userReg;
    protected async override Task OnInitializedAsync()
    {
        loginState.OnChange += StateHasChanged;

        if (loginState.gFileId > 0)
        {
            var x = loginState.gFileId;
        }


    }

    public void Dispose()
    {
        loginState.OnChange -= StateHasChanged;
    }

}
